import { OidcLoginInputModel } from './oidc-login-input-model';

describe('OidcLoginInputModel', () => {
  it('should create an instance', () => {
    expect(new OidcLoginInputModel()).toBeTruthy();
  });
});
