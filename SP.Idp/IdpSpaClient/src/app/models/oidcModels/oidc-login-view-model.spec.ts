import { OidcLoginViewModel } from './oidc-login-view-model';

describe('OidcLoginViewModel', () => {
  it('should create an instance', () => {
    expect(new OidcLoginViewModel()).toBeTruthy();
  });
});
